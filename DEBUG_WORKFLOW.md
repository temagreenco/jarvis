# Debug workflow (Jarvis)

This doc is the canonical checklist for running jobs and pulling artifacts locally.

## Quick sanity
```powershell
docker compose ps
curl.exe -sS http://localhost:8000/health
